one:
  user_id: <%= users(:one).id %> # This will reference the "one" user from users.yml
  content: Sample tweet content for user one.

two:
  user_id: <%= users(:two).id %> # This will reference the "two" user from users.yml
  content: Another sample tweet for user two.